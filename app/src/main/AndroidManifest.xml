<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sample.hellocompose">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HelloCompose">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.HelloCompose.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".screen1.Screen1Activity" />
        <activity android:name=".screen1.Screen1ComposeActivity" />

        <activity android:name=".screen2.Screen2Activity" />
        <activity android:name=".screen2.Screen2ComposeActivity" />

        <activity android:name=".screen3.Screen3Activity" />
        <activity android:name=".screen3.Screen3ComposeActivity" />

        <activity android:name=".screen4.Screen4Activity" />
        <activity android:name=".screen4.Screen4ComposeActivity" />
    </application>

</manifest>
